# CLAUDE.md

Richtlijnen voor het werken in deze codebase. Lees dit voordat je wijzigingen maakt.

## Wat is dit
SportCoach AI — een Nederlandstalige AI-sportcoach-webapp (Next.js, PWA). Oorspronkelijk voor één atleet (Taco), sinds 2026-06-04 **multi-user**: vrienden koppelen hun eigen Garmin-account. Integreert Garmin-data, dagelijkse check-ins, AI-coach chat (Claude), weekrapporten, MyFitnessPal-voeding, materiaal-tracking, multisport en een wedstrijd-dashboard (tabblad "Races").

## Commando's
- `npm run dev` — lokale dev-server
- `npm run build` — productie-build (draai dit vóór elke push als sanity-check)
- `npm run lint` — ESLint
- `npx tsc --noEmit` — type-check (geen apart script; draai handmatig)

## Stack
- Next.js 16 (App Router), React 19, TailwindCSS 4
- `@anthropic-ai/sdk` voor Claude-calls
- `garmin-connect` voor Garmin-data
- `next-pwa` voor PWA
- Gehost op **Vercel** (project `sportcoach-ai`). Push naar `main` → auto-deploy.

## Architectuur & conventies
- **Taal:** alle UI en gebruikerstekst is **Nederlands**. Houd dit aan.
- **Opslag:** alles via `src/lib/storage.ts` (localStorage). Niet direct localStorage aanroepen elders.
- **Types:** centraal in `src/lib/types.ts`. Voeg nieuwe types daar toe.
- **AI-modellen:** Haiku (`claude-haiku-4-5-20251001`) voor snelle feedback/JSON-formattering, Opus 4.8 (`claude-opus-4-8`) voor coach-chat én de schema-redenering, Sonnet (`claude-sonnet-4-6`) voor snelle dag-aanpassingen (`adjust-day`). Zie `src/app/api/*`. **Vercel-timeout:** standaard 10s; verleng met `export const maxDuration` als een endpoint Opus gebruikt (chat=30, generate-plan=60). Opus' nieuwe thinking-API = `thinking: { type: 'adaptive' }` + `output_config: { effort }` (niet `budget_tokens`).
- **Navigatie:** tab-layout in `src/components/Navigation.tsx`.
- **Pure logica** in `src/lib/*` (bijv. `equipment.ts`, `swim.ts`, `training-load.ts`, `periodization.ts`, `races.ts`), UI in `src/components/*`.

## Belangrijke domeinkennis
- **Trends:** wandelen telt NIET mee in hardloop-trends (exacte match `sport === 'hardlopen'`). Stadsfiets-ritten tellen niet mee in trainingsstatistieken (`filterStatsActivities` in `equipment.ts`).
- **Materiaal:** fiets-types `racefiets | mountainbike | stadsfiets` + `hardloopschoenen`. Stadsfiets heeft bewust geen onderhoud. Legacy `'fiets'` wordt gemigreerd naar `racefiets`.
- **Zwemmen:** varianten binnen/buiten/openwater per activiteit.
- **Multisport (triatlon/duatlon/brick):** Garmin slaat dit op als **parent met child-activiteiten**. De disciplines zitten NIET in `/splits`, maar in `metadataDTO.childIds` → per child `activityTypeDTO.typeKey` + `summaryDTO`. Zie `src/app/api/garmin/sync/route.ts`.
- **Wedstrijden (tabblad "Races", `/wedstrijden`):** races komen uit het **Doelen-systeem** (geen aparte opslag); `src/lib/races.ts` koppelt elk `Goal` aan de Garmin-activiteit van ±1 dag voor echte splits/pace/HR. Weer via `src/app/api/weather/route.ts` (Open-Meteo, geen API-key), gecached per goalId.
- **Activiteiten-archief:** de Garmin-sync overschrijft de live-data, dus voor historische trends (bijv. de aanloop naar een wedstrijd) is er een **groeiend archief** in `storage.ts` (`ACTIVITY_ARCHIVE` + `HEALTH_ARCHIVE`, dedup). De sync haalt nu 150 activiteiten op; beide sync-handlers (`page.tsx`, `data/page.tsx`) mergen álles in het archief en trimmen de live-weergave tot 40.
- **Schema-generatie (`/schema/nieuw` → `/api/generate-plan`, generate-mode):** twee-traps. **Trap 1** = Opus 4.8 met extended thinking schrijft een coachstrategie (geen JSON) op basis van de **prestatie-samenvatting** van de afgelopen weken; **trap 2** = Haiku zet die strategie om naar gevalideerde JSON. De prestatie-samenvatting komt uit `src/lib/performance-summary.ts` (`buildPerformanceSummary`): **8-weekse** breakdown uit het archief (volume/afstand/HR per sport, TRIMP-trend, herstel-trend, wedstrijdresultaten per week), stadsfiets uitgesloten via `filterStatsActivities`. De API geeft naast `plan` ook `strategy` terug; die toont de UI als inklapbare "Waarom dit schema"-kaart (markdown opgeschoond met `cleanStrategyText`). Duurt ~40s — bewust. De refine-mode en `adjust-day` blijven snel (Haiku/Sonnet, één call).
- **Meerdere wedstrijden / jaarplanning (2026-06-04):** het Doelen-systeem ondersteunt meerdere actieve doelen tegelijk — geen "hoofddoel", alle wedstrijden zijn gelijkwaardig. `getUpcomingGoals()` geeft alle actieve doelen met datum ≥ vandaag gesorteerd op datum. `getActiveGoal()` kiest altijd de dichtstbijzijnde toekomstige race (voor countdown, periodisering, schema-focus). `buildRaceContextText()` geeft de volledige wedstrijdkalender aan de AI, met "← VOLGENDE" markering. `getPendingResultGoal()` checkt alle voorbije actieve doelen zonder resultaat. De `GoalsSection` toont een gerangschikte lijst: bovenste = volgende wedstrijd (blauw), de rest wit. Formulier-modals gebruiken nu **flex-column layout** (header/footer `flex-shrink-0`, body `flex-1 overflow-y-auto`) zodat de Opslaan-knop altijd zichtbaar is op mobiel.
- **Afstanden per discipline (2026-06-04):** multisport-doelen (triatlon, duatlon) hebben optioneel `disciplineDistancesKm: { swim?, bike?, run?, run2? }` (km). Ingevuld via het bewerkformulier (velden verschijnen alleen bij multisport-types; duatlon heeft een tweede looponderdeel). Worden opgenomen in `buildRaceContextText()` en `buildGoalsHistoryText()` voor AI-context.
- **Sport-specifieke hartslagzones (2026-06-04):** `UserProfile` heeft `hrZonesRun` en `hrZonesCycling` (`HRZoneConfig`: z1min–z5min + maxHR). Instelbaar via "Hartslagzones instellen" op de Data-tab (tabel met Z1–Max HR × hardlopen/fietsen, Opslaan-knop). `getRunZones()` / `getCyclingZones()` in `storage.ts` lossen de config op (aangepast of berekend via `computeHRZones(maxHR)` als fallback). `buildHRZoneText()` genereert sport-specifieke zone-tekst voor alle AI-prompts (generate-plan, adjust-day, chat). Schema-tab toont twee aparte zone-tabellen. `HEART_RATE_ZONES` blijft als backward-compat constante (hardlopen, max HR 172).
- **Multi-user / Garmin-koppeling (2026-06-04):** Garmin-credentials zitten **niet** meer hardcoded op de server. Elke gebruiker vult email+wachtwoord in via `GarminSetupCard` (Data-tab), opgeslagen in localStorage (`GARMIN_CREDENTIALS`, helpers `getGarminCredentials/saveGarminCredentials/clearGarminCredentials`). Beide sync-handlers (`page.tsx`, `data/page.tsx`) sturen de credentials mee in de POST naar `/api/garmin/sync`; de route leest ze uit het request body met **env vars als fallback** (`GARMIN_EMAIL/PASSWORD`). Auto-sync op de homepage wordt overgeslagen als er geen credentials in localStorage staan. Let op: Garmin met MFA aan geeft "login failed (Ticket not found or MFA)" — de `garmin-connect` library ondersteunt MFA niet, gebruiker moet 2FA uitzetten.
- **Cyclus vóór startdatum:** `getCurrentWeekNumber`/`getTrainingForDayOffset` in `schedule.ts` geven **week 1** als `cycleStartDate` in de toekomst ligt (negatieve `diffDays`) — anders gaf JS-modulo op negatieve getallen ten onrechte week 2.
- **Cyclus uitlijnen op de race (2026-06-08):** `getActivePlan()` in `storage.ts` corrigeert bij het inlezen de `cycleStartDate` via `alignCycleStartToRace()`: als de race binnen de cyclus valt (`0 ≤ daysUntilRace < weken×7`), wordt de week mét de race aan de **laatste planweek** gekoppeld (wedstrijdweek = week 2). Anders blijft de opgeslagen datum staan. Past géén plan-inhoud of opgeslagen data aan — alleen de read-time ankerdatum, dus home/coach/schema (die allemaal `getActivePlan` gebruiken) tonen meteen de juiste "huidige week". Reden: `approvePlan` zet `cycleStartDate = getNextMonday()` met week 1 vooraan, waardoor de wedstrijdweek anders een week te laat "huidig" werd. Rekent in **UTC** (`...T00:00:00Z` + `getUTCDay`) om tijdzone-drift in de ankerdatum te vermijden.
- **Grafieken:** `src/components/BuildupBarChart.tsx` (staafdiagram, wedstrijd-aanloop + Data-trends), `TrendLineChart.tsx` (lijn), `WeeklyVolumeChart.tsx` (gestapeld staafdiagram volume per sport: zwem=blauw/fiets=groen/run=oranje). Alle drie hebben **hover/touch-tooltips** (React-state + absoluut gepositioneerde overlay-div, dichtstbijzijnde staaf/punt via x-positie). De **Data-tab Trends** gebruikt `BuildupBarChart` op een `bg-gray-100`-kaart, in volgorde: volume per sport → rusthartslag → HRV → gem. HR/tempo/speed/power/zwemtempo. Rusthart+HRV komen uit `HEALTH_ARCHIVE` (`getHealthArchive`), volume uit `ACTIVITY_ARCHIVE` (`getActivityArchive`).
- **Gedeelde Garmin-sync (2026-06-06):** `syncGarminData()` in `storage.ts` is de **canonieke** sync (delta-fetch, hrZones behouden, archief-merge, live-weergave trimmen tot 40, `markAutoSyncDone`). Gebruikt door de check-out (`CheckInForm`/`CheckInContent`); homepage `page.tsx` heeft nog z'n eigen `handleGarminSync` (stuurt creds mee). Geeft verse data terug of throwt bij mislukte sync (caller valt terug op cache).
- **Check-out coach (`/coach`, tab "Check-out", 2026-06-06):** `CheckInContent` **synct Garmin bij het openen** (cache eerst tonen, dan vers; sync-indicator) zodat de activiteiten van vandaag direct zichtbaar zijn. Drie situaties: (1) trainingsdag → gepland-vs-gedaan + check-out-formulier; (2) **rustdag mét Garmin-activiteit** → toont "Rustdag — maar je hebt toch getraind" + activiteit + check-out (lege `sessions`, coach krijgt rustdag-specifieke opdracht: focus op herstel i.p.v. geplande doelen); (3) rustdag zonder activiteit → rust-boodschap. **Al ingecheckt is geen dead-end meer:** `CheckInForm` krijgt een `resumeCheckIn`-prop die het gesprek heropent met de eerdere coach-berichten zodat je kunt doorvragen. Activiteiten-kaartjes via gedeelde `TodayActivities`-helper.
- **Coach van de dag (`/api/daily-message`, 2026-06-06):** (a) **timing-fix** — elke recente activiteit krijgt een expliciet dag-label (`[VANDAAG]`/`[GISTEREN]`/`[WEEKDAG, X DAGEN GELEDEN]`) op basis van echte datum vs. vandaag, met harde regel "noem iets alleen 'gisteren' bij `[GISTEREN]`, anders de weekdag" — anders gokte Haiku "gisteren" voor alles. (b) **sync-first** — `page.tsx` forceert een Garmin-sync vóór het eerste dagbericht van de dag (zolang er creds zijn); de `syncing`-gate laat de generatie wachten tot de sync klaar is. De vernieuw-knop synct nu ook eerst (`refreshDailyMessage`). Cache 1x/dag (`getDailyMessage`, key = Amsterdam-datum). Model: Haiku.
- **Home-tab donker "Whoop/Liquid Glass"-design (2026-06-25):** als enige tab donker thema (`bg-black`), de rest van de app blijft licht. De pagina is gesplitst: `src/app/page.tsx` is nu een **server component** die alleen `viewport.themeColor = '#000000'` overschrijft voor deze route (andere tabs houden het globale blauw `#2563eb` uit `layout.tsx`); de eigenlijke UI (alle hooks/state) staat in `src/components/HomeContent.tsx` (`'use client'`). Root-`viewport` heeft nu ook `viewportFit: 'cover'` zodat `env(safe-area-inset-top)` een echte waarde teruggeeft; `HomeContent` vult die strook zelf zwart met een `fixed top-0` div, anders blijft de notch/statusbar-achtergrond licht. `Countdown.tsx` (hero met countdown + fasetijdlijn) is `rounded-t-3xl` (rechte overgang naar de kaarten erna, geen overlap meer). **Gereedheid + Training load** staan naast elkaar (`grid grid-cols-2`); `ScoreRing` heeft een `size`-prop met `compact`-modus (< 130px verbergt het sublabel). **Volume deze week**: per sport-rij staat label+afstand op een eigen regel (`whitespace-nowrap`, anders loopt het door elkaar, bv. "Zwemmen1.0"/"km" op losse regels) en een sport zonder activiteit die of vorige week toont **geen rij** (`hasData`-filter in `volumeComparison`). De oude "Laatste check-out"-sectie op de Home-tab is verwijderd.
- **Home-tab "Laatste activiteit" met match-score (2026-06-27):** onderaan de Home-tab (ná "Volume deze week") staat `src/components/LatestActivityCard.tsx` — de meest recente activiteit uit `statsActivities[0]` (stadsfiets uitgesloten via `filterStatsActivities`). Toont duur/afstand/gem. HR (met de zone waarin de gem. HR valt) + een zone-verdeling-balkje uit `activity.hrZones`, en een **match-score (0–100%)** die uitdrukt hoe goed de uitvoering aansloot op de **geplande sessie van die dag**. Score = `computeActivityMatchScore()` in `training-load.ts`: **50% duur-adherentie** (`100 - |1 - gedaan/gepland|·150`) + **50% zone-adherentie** (zelfde zone=100, één ernaast=65, verder=25), met label "Precies volgens plan" (≥85) / "Redelijk uitgevoerd" (≥60) / "Flink afgeweken". De geplande sessie wordt gekoppeld via `getTrainingForDayOffset(-daysSince, …)` op de activiteit-datum, dan de sessie met dezelfde `sport`. Geen geplande training (of rustdag) → alleen de activiteit, geen score. Zone-resolutie sport-specifiek via `getRunZones()`/`getCyclingZones()`. Helpers `computeActivityMatchScore`/`getHRZone` zitten in `training-load.ts`.
- **Schema-tab donker "Whoop/Liquid Glass"-design (2026-06-26):** zelfde patroon als Home — `src/app/schema/page.tsx` is een server component die alleen `viewport.themeColor = '#000000'` zet; de UI staat in `src/components/SchemaContent.tsx` (`'use client'`). De tab opent **direct bovenaan** met een gedetailleerde "vandaag/morgen"-sectie (`DetailedDay`/`DetailedSession`-helpers): per onderdeel een Sport/Type/Duur-grid + exacte hartslagzone in bpm + de volledige omschrijving, bedoeld om over te nemen in een Garmin-horloge. De dag-titel toont de **echte weekdagnaam** (bv. "Zaterdag") i.p.v. het label "Morgen" (dat als subtitel staat). Er is **geen auto-scroll-naar-vandaag** meer in de weekweergave (die scrollde voorheen voorbij de nieuwe boven-sectie); de tab blijft simpelweg boven staan. Koppen "Trainingsschema" en "Voor je Garmin — vandaag & morgen" zijn verwijderd — de sectie spreekt voor zich. **Tekststijl:** alle gewone tekst (sport/type/duur-stats, dagkoppen, doelnamen, aanpas-modal) gebruikt de zachtere "coach-stijl" (`text-gray-100`/`font-medium`/`font-semibold`) net als "Coach van de dag" op Home, **behalve hartslagzone-elementen** (zone-badges, legendetabellen) — die blijven fel/wit voor leesbaarheid. `SportIcon` kreeg een `2xl`-maat (80px) voor zo groot mogelijke icoontjes. `TrainingCard` en `GoalsSection` hebben een `dark`-prop (sport-specifieke zones, type/duur/zone-badges, doelenlijst/archief/fasetijdlijn); formulier-modals blijven licht.
- **Data-tab sub-navigatie (2026-06-27):** het Data-tab (`src/app/data/page.tsx`, licht thema) was één lange scroll en is opgesplitst in **vier secties** via een segmented control (`section`-state: `'overzicht' | 'trends' | 'activiteiten' | 'instellingen'`): **Overzicht** (trainingsgereedheid, deze week, training load, gezondheid), **Trends** (belasting-grafiek + de 8-weken trend-grafieken), **Activiteiten** (`MaterialSection` + activiteitenlijst) en **Instellingen** (Garmin-koppeling, hartslagzones, data beheer). De **Synchroniseer Garmin-knop** staat alleen in Overzicht + Instellingen (niet bij Trends/Activiteiten). De gedupliceerde "Hartslagzones instellen"- en "Data beheer"-blokken (eerder copy-paste in zowel de met- als zonder-Garmin branch) zijn herbruikbare componenten geworden: `src/components/HeartRateZonesCard.tsx` en `src/components/DataManagementCard.tsx`. **Ronden/laps per activiteit** zijn nu inklapbaar (`expandedSplits` Set-state, standaard ingeklapt; knop "Toon ronden (N)"/"Toon disciplines (N)" ↔ "Verberg onderdelen"). `GarminSetupCard` is van het donkere `bg-gray-800` naar **lichte stijl** (wit + grijze rand) omgezet zodat het bij de lichte Data-tab past — wordt alleen daar gebruikt.
- **Data-tab "Materiaal" als eigen subtab (2026-06-27):** de "Activiteiten"-subtab bevatte zowel `MaterialSection` als de activiteitenlijst; die zijn gesplitst in **twee aparte subtabs**. `Section`-type heeft nu `'materiaal'` erbij, de segmented control telt **vijf tabs** (Overzicht · Trends · Activiteiten · Materiaal · Instellingen) en de knoppen kregen `text-xs` + `px-0.5` + `whitespace-nowrap` zodat ze op smal scherm (390px) op één rij passen. `MaterialSection` rendert nu onder `section === 'materiaal'`, de activiteitenlijst onder `section === 'activiteiten'`. Sync-knop blijft alleen in Overzicht + Instellingen.
- **Home-tab "Laatste activiteit" uitklapbaar (2026-06-27):** `src/components/LatestActivityCard.tsx` heeft onderaan een **"Toon details"/"Verberg details"-toggle** (`expanded` useState, alleen getoond als er details zijn). Uitgeklapt verschijnen: een grid met **extra stats** (max HR, tempo/snelheid, vermogen+NP, calorieën, hoogtemeters, cadans run/bike, VO₂max, aeroob/anaeroob TE — elk alleen als er data is), **tijd per zone** in minuten (gekleurde balkjes per zone uit `activity.hrZones`, sport-specifieke kleuren via `zonesForSport`), en de **ronden/laps** (`activity.splits`, met afstand/tijd/HR/W). Donker thema, consistent met de rest van de Home-tab.
- **Kracht/workouts in het schema (2026-06-29):** nieuwe sport `'kracht'` (rose halter-icoon via `SportIcon`, `bg-rose-500`, **geen hartslagzone** — kracht werkt met sets/reps/tijd). Toegevoegd aan `Sport`, `SPORT_ICONS`/`SPORT_COLORS` (`types.ts`), de `VALID_SPORTS`/JSON-spec van beide AI-routes en de SPORT_LABEL-maps. **Planning via AI:** `generate-plan` (Opus-strategie + Haiku-JSON) en `adjust-day` (Sonnet) krijgen de opdracht per week **3× core (type "core", 7 min) op lichtere dagen + 2× krachttraining (type "kracht", ~40 min) op triatlontrainingsdagen** in te plannen, als 2e sessie, nooit op een rustdag. **Schema-tab:** kracht toont als gewone sessie (Sport/Type/Duur, geen zone) via `TrainingCard`/`DetailedSession`. **Home-tab onder "Training vandaag":** krachtsessies tonen een **vaste oefenlijst** (geen AI, geen zones) i.p.v. de AI-breakdown — `TodayTrainingDetail` filtert kracht uit de `session-breakdown`-fetch en rendert `src/components/StrengthWorkoutDetail.tsx`. **De oefeningen komen NIET van een model:** vaste bibliotheek in `src/lib/strength.ts` (`DEFAULT_STRENGTH_WORKOUTS`: 7-min core-circuit + triatlon-kracht ~40min), keuze via heuristiek `pickStrengthWorkoutId` (regex op type/omschrijving + duur). Kracht telt niet mee in `estimatePlannedTRIMP` (geen aerobe belasting). Check-out-prompt (`CheckInForm`) toont geen lege "in undefined"-zone meer.
- **Hele app donker "Whoop/Liquid Glass" (2026-07-03):** Races, Voeding en Data zijn omgezet naar hetzelfde donkere thema als Home/Schema/Coach — de hele app is nu donker. Dit VERVANGT de eerdere aanpak "alleen Home/Schema donker": `viewport.themeColor` staat nu **globaal** op `#000000` in `layout.tsx` (de per-route viewport-overrides in `page.tsx`/`schema/page.tsx`/`coach/page.tsx` zijn verwijderd) en `--background` in `globals.css` is zwart (`--foreground` blijft donker als default voor de lichte formulier-modals). Kaart-idioom: `bg-[#0d0d0f] rounded-3xl border border-white/5`, koppen `text-white`, secundair `text-gray-400/500`, inputs `bg-white/5 border-white/10 text-white`. Omgezet: `wedstrijden/page.tsx` + `[id]/page.tsx`, `voeding/page.tsx`, `data/page.tsx` en de kaart-componenten `GarminSetupCard` (terug naar donker), `HeartRateZonesCard`, `DataManagementCard`, `StrengthWorkoutsCard`, `MaterialSection` (kaarten; formulier-modal blijft licht), `RaceSplitBar`, `WeatherCard` + de grafieken `BuildupBarChart`/`TrainingLoadChart`/`WeeklyVolumeChart` (rasterlijnen `rgba(255,255,255,0.06-0.15)`, aslabels `#9ca3af`/`#6b7280`). **Formulier-modals en de chip-kiezers (EquipmentAssignChip/SwimVariantChip-modals) blijven licht** — alleen de chip-badges zelf zijn donker. Kleine fixes in dezelfde ronde: (a) Races: afgeronde race zonder tijd toont een amber "Vul tijd in"-knop (deep-link naar het resultaatformulier) i.p.v. grijs "geen tijd"; (b) Voeding: de dubbele lege-staat (importkaart + "nog geen data"-kaart) is één kaart; (c) **Data zonder Garmin-data heeft nu ook de vijf-subtab-navigatie** (geen aparte fallback-pagina meer; Overzicht toont dan de GarminSetupCard + lege staat, Instellingen zit op de vaste plek); (d) "Coach van de dag" laadt met een shimmer-skeleton i.p.v. bouncende puntjes.
- **Na-de-race-flow + AI-race-evaluatie (2026-07-03):** na de racedatum is de app niet langer stuurloos. `Countdown` toont dan een overgangs-hero ("{race} zit erop!") i.p.v. een countdown: zonder resultaat een "Vul je resultaat in"-CTA, mét resultaat de eindtijd + "Kies je volgende doel"; de Home-hero deep-linkt naar het resultaatformulier (`/schema?tab=longterm&goal=…`). `GoalsSection` toont verlopen actieve doelen zonder resultaat als amber **"Wacht op resultaat"-blok** — die waren voorheen onzichtbaar (de lijst filtert op datum ≥ vandaag). Na het opslaan van een resultaat genereert **`/api/race-evaluation`** (Opus 4.8, `maxDuration 30`) automatisch een coach-evaluatie uit doel+resultaat+splits (Garmin via `buildRaces`/`getRaceSplits`, anders handmatige splits) + de trainingsaanloop (`getPreRaceBuildup`); opslag in `GoalResult.aiEvaluation` (`saveGoalEvaluation`), weergave in een evaluatie-modal (met "Kies je volgende doel"-knop als er geen aankomende race is), het doelen-archief (incl. "✨ Genereer coach-evaluatie"-knop voor oude races met resultaat) en de wedstrijd-detailpagina. AI-context: `buildRaceContextText()` geeft zonder aankomende race een expliciete herstel-/overgangsinstructie i.p.v. "(voorbij)", `getCurrentPhase()` geeft ná de racedag `POST_RACE_PHASE` ("Herstel- en overgangsfase" — bewust NIET in `TRAINING_PHASES`, dus tijdlijnen ongewijzigd), en het chat-welkomstbericht zegt niet langer "richting je race op {verleden datum}". Bugfix onderweg: `CoachContent` en `schema/nieuw` riepen `getCurrentPhase()` zonder argument aan (hardcoded fallback 13 juni) — nu met `getActiveRaceDate()`.
- **Zwemtempo per 100m i.p.v. bpm (2026-07-03):** hartslag is in het water onbruikbaar als stuurmiddel, dus zwemsessies tonen nu een **richttempo per 100m**. `estimateSwimPaceTargets()` in `swim.ts`: mediaan-tempo uit recente zwemtrainingen in het archief (max 10, ≤120 dagen oud, ≥0,4 km, plausibiliteit 60–240 s/100m; minimaal 2 nodig), zone-offsets t.o.v. dat basistempo (Z1 +8..+14s … Z5 −8..−4s); `getSwimPaceTargets()` in `storage.ts` leest het archief. Badges aangepast in `TrainingCard` (dark + light), `SchemaContent` `DetailedSession` (tempo-rij i.p.v. bpm-rij, "tempo op gevoel" als fallback) en `TodayTrainingDetail` (sessie- én segment-badges). Zonder zwemdata toont zwemmen alleen zone+label — nooit meer bpm-getallen (dat waren run-zones). Schema-tab: nieuwe **"Zwemtempo's (per 100m)"-legenda** onder de HR-zonetabellen, met bronvermelding (aantal trainingen + gemiddeld tempo).
- **Plan-adherentie op weekniveau (2026-07-03):** `computeWeekAdherence()` in `training-load.ts` vergelijkt gepland vs. gedaan over de **laatste 7 volledige dagen** (gisteren t/m −7; vandaag telt niet mee omdat de training nog kan komen). Geplande sessies per dag via `getTrainingForDayOffset`, gematcht op sport (fietsen↔mountainbike uitwisselbaar), uitvoeringskwaliteit per gedane sessie via `computeActivityMatchScore`; **kracht uitgesloten** (geen betrouwbare Garmin-registratie). Weergave: Data-tab → Overzicht kreeg een **"Volgens plan"-kaart** (percentage + label, X van Y sessies, gem. uitvoering, voortgangsbalk, per dag een stip per sessie: groen=gedaan/rood=gemist). Het weekrapport (`WeeklyReportSection` → `/api/weekly-report`) stuurt `adherence` mee (incl. lijst gemiste sessies) en bespreekt die in punt 1 van het rapport. Berekening gebruikt het archief (niet de live-40) via `filterStatsActivities`.
- **Krachtoefeningen aanpasbaar (2026-06-29):** de twee kracht-workouts zijn volledig bewerkbaar. Editor `src/components/StrengthWorkoutsCard.tsx` op de **Data-tab → Instellingen** (licht thema, naast `HeartRateZonesCard`; ook in de `!garmin`-fallback-branch van `data/page.tsx` toegevoegd — anders mist hij bij gebruikers zonder Garmin-data). Per oefening: naam/voorschrift/techniektip wijzigen, toevoegen (+), verwijderen (✕), herordenen (▲▼); plus introtekst en blok-labels; per workout **Opslaan** of **Standaard** (reset). Opslag via `storage.ts`: `getStrengthWorkouts`/`saveStrengthWorkout`/`resetStrengthWorkout`/`getStrengthWorkoutForSession` (key `STRENGTH_WORKOUTS`), met `DEFAULT_STRENGTH_WORKOUTS` als terugval voor niet-aangepaste workouts. `cloneStrengthWorkout` voor veilig bewerken zonder de defaults te muteren. `TodayTrainingDetail` leest nu via `getStrengthWorkoutForSession` (aangepast of standaard). Snelkoppeling: "✎ Oefeningen aanpassen"-link onderaan de Home-kracht-kaart deep-linkt naar `/data?section=instellingen` (data-page leest die query-param via `window.location.search`, niet `useSearchParams`).

- **Zwemtempo's Z1–Z5 handmatig instelbaar (2026-07-03):** de zwemtempo-zones die op de Schema-tab in het overzicht staan, zijn nu direct aanpasbaar op **Data → Instellingen** (`src/components/HeartRateZonesCard.tsx`, kaart hernoemd naar "Zones & tempo's instellen"). Onder de HR-tabel staat een tabel met per zone een **Snel**- en **Rustig**-veld (min:sec /100m). Voorgevuld met de auto-schatting uit het archief; opslaan zet ze vast in `UserProfile.swimPaceZones` (`SwimPaceZone[]` in `types.ts`, volgorde Z1..Z5). `getSwimPaceTargets()` (`storage.ts`) gebruikt die override met voorrang op `estimateSwimPaceTargets`; het oude losse basistempo-veld is vervangen. Nieuwe helpers in `swim.ts`: `parseSwimPace` (accepteert "2:05" of seconden, plausibiliteit 60–240s), `buildSwimPaceTargetsFromZones`, en `source: 'auto' | 'handmatig'` op `SwimPaceTargets`. Validatie: alle 10 velden geldig én snel ≤ rustig, anders foutmelding en geen opslag; opslaan zonder wijziging t.o.v. de schatting blijft "auto" (niet onnodig bevriezen). "Terug naar automatisch"-knop wist de override. Schema-tab-legenda toont bij handmatige waarden een aangepaste bronvermelding. Alle bestaande consumers (`TrainingCard`, `TodayTrainingDetail`, `SchemaContent`) volgen automatisch via dezelfde `getSwimPaceTargets`.

- **Triatlon-kracht afgestemd op thuis-krachtstation (2026-07-04):** de `TRI_STRENGTH`-default in `src/lib/strength.ts` is herschreven rond het beschikbare materiaal van de gebruiker (multi-gym krachtstation met lat-pulldown/chest-press-armen/lage katrol/leg-extension+leg-curl, plus elastische banden + matje). Opgebouwd als supersets: WU (foam roll + band pull-aparts) → Superset A (leg extension + lat pulldown) → B (leg curl + chest press) → C (Bulgarian split squat + seated row) → prehab/core (1-benige RDL, face pulls, kuitheffing, pallof press) → cooldown. Dekt alle drie disciplines + blessurepreventie in ~40 min, 3×12–15, 60–90s rust (off-season 4×6–8). Alleen data (de constante), geen structuurwijziging; verschijnt automatisch op Home (`StrengthWorkoutDetail`) en Schema op krachtdagen. Elke oefening is 1-op-1 in te voeren als Garmin Connect **Strength**-workout (categorie→oefening→reps/gewicht/rust; geen HR-zone). Let op: een via de editor aangepaste `tri-strength` in localStorage overschrijft deze default — dan "Standaard" klikken. `core7` bleef ongewijzigd.
- **Schema aanpas-modal achter navbar (2026-07-04):** de "Dag aanpassen"-modal op de Schema-tab (`src/components/SchemaContent.tsx`, potlood-icoon per dag → `adjustDay`) had z-index `z-50` — exact gelijk aan de bottom-navigatiebalk (`Navigation.tsx`, ook `z-50`). Bij gelijke z-index wint het later-in-de-DOM-element, dus de navbar prikte door de modal-achtergrond heen en bedekte de Annuleren/Aanpassen-knoppen (zowel op normale hoogte als met open toetsenbord). Fix: modal-overlay naar `z-[60]` zodat de modal altijd boven de navigatie ligt. Tegelijk kreeg de modal het standaard flex-column-idioom (`max-h-[85vh] flex flex-col`, header/footer `flex-shrink-0`, body `flex-1 overflow-y-auto`) zodat lange inhoud scrollt i.p.v. de knoppen weg te duwen.
- **Check-out-bugfixes (2026-07-04):** twee losstaande bugs op de Coach-tab → Check-out gefixt. (a) **Coach-antwoord verdween stil bij een mislukte `/api/chat`-call** — `fetchFeedback`/`sendMessage` in `src/components/CheckInForm.tsx` deden niets bij `response.ok === false` en hadden een lege `catch`, waardoor de laadindicator verdween zonder enig bericht; nu tonen beide een assistant-foutbericht ("Sorry, er ging iets mis...") zodat de gebruiker altijd feedback ziet. (b) **"Terug naar dashboard"-knop ging niet naar het dashboard** — `onComplete` in `src/components/CoachContent.tsx` schakelde alleen naar de Chat-subtab binnen Coach (`setActiveTab('chat')`) i.p.v. te navigeren naar de Home-tab; nu `router.push('/')` (`useRouter` uit `next/navigation`).
- **Warming-up/cooldown zonder zonedoel op Home (2026-07-05):** in het gedetailleerde schema op de Home-tab (`src/components/TodayTrainingDetail.tsx`, `SegmentRow`) tonen warming-up- en cooldown-segmenten geen hartslagzone-badge meer — alleen de tijd. Reden: WU/CD stuur je op gevoel/tijd, een zonedoel is daar zinloos. Alleen weergave: de `/api/session-breakdown` genereert intern nog wel een zone voor die segmenten (blijft gecachet), die wordt simpelweg niet meer gerenderd (`isWarmupCooldown` → `zoneInfo = null`). De gekleurde accent-streep (groen WU / grijs CD) blijft. Hoofdblokken houden hun zone-badge. De Schema-tab is bewust ongewijzigd: die splitst sessies niet in WU/CD-segmenten, de enige zone-badge daar is de hoofdzone van de sessie.
- **Bovenkant viel weg onder de notch/statusbalk (2026-07-06):** in de PWA verdwenen de bovenste regels van elke tab achter de iPhone-statusbalk/notch. Oorzaak: `viewportFit: 'cover'` (nodig om de notch-strook zwart te kleuren) laat de webview onder de statusbalk doorlopen, maar de inhoud werd nergens naar beneden geduwd — de hero begon met slechts `pt-6` terwijl de safe-area bovenaan ~47–59px is. Elke tab tekende wel een cosmetische zwarte strip (`fixed top-0 … bg-black`) over de notch, maar dat verschuift de inhoud niet. Fix op één plek: nieuwe `.pt-safe`-utility in `src/app/globals.css` (`padding-top: env(safe-area-inset-top, 0px)`) toegepast op `<main>` in `src/app/layout.tsx` (`pt-safe pb-safe`), zodat de inhoud van álle tabs onder de notch begint. Op desktop is de inset 0 → geen zichtbaar verschil; effect alleen op de telefoon (PWA vanaf thuisscherm). De per-tab zwarte strips zijn nu strikt genomen overbodig (body is al zwart) maar bewust behouden.
- **Readiness-sync viel stil weg (2026-07-05):** HRV/slaap/body battery/rusthartslag kwamen niet meer binnen terwijl de activiteiten-sync bleef werken. Oorzaak in `src/app/api/garmin/sync/route.ts`: álle readiness-data komt uit één `GC.getSleepData()`-call, die samen met `GC.getSteps()` en `getUserSettings()` in **één `Promise.all` binnen één `try/catch`** zat. `Promise.all` is alles-of-niets: zodra `getSteps()` faalde (de `garmin-connect`-library gooit `"Can't find daily steps for this date."` als er vandaag nog geen stappen-record is) werd de héle readiness stil naar `health = null` gegooid — geen UI-signaal (`mergeHealthIntoArchive(null)` is een no-op). Fix: elke bron wordt nu **los** opgehaald met eigen `try/catch` (één faler sloopt de rest niet), slaap heeft een **yesterday-fallback** als vandaag nog leeg is, en er is gedetailleerde `[readiness]`-logging per endpoint (keys + `avgOvernightHrv`/`hrvStatus`/`restingHeartRate`/`bodyBatteryChange`/`sleepTimeSeconds`, het opgebouwde health-object, en een expliciete `warn` als er echt geen slaapdata is). Geen scope/auth-probleem: dezelfde sessie draagt zowel activiteiten als wellness, dus als de sessie verlopen was zou de activiteiten-sync óók falen. **Vervolg (wat het écht oploste):** de robuustere `getSleepData()` alleen was niet genoeg — de readiness bleef leeg. De sleep-DTO is één brekbare bron. Nu halen we HRV/rusthart/body battery **primair uit Garmins eigen wellness-services** (`hrv-service/hrv/{datum}`, `metrics-service/metrics/trainingreadiness/{datum}`, `usersummary-service/usersummary/daily/{displayName}?calendarDate=…` — `displayName` via `getUserProfile()`), met de sleep-DTO als fallback. Elk endpoint los met `try/catch` + `[readiness]`-logging; `health` wordt opgebouwd zodra **één** bron data geeft (`hasAnyData`-gate). Dit zijn dezelfde endpoints die de Garmin-app voor het readiness-scherm gebruikt. Diagnose verliep via de Vercel-logs (project `sportcoach-ai` → Logs, filter `readiness`); let op: Hobby-plan bewaart maar **1 uur** logs en `console.log` verschijnt alleen bij een **verse server-sync** (de app valt bij een mislukte/oude sync terug op localStorage-cache, dus "leeg" kan ook een cache-artefact zijn). Getest: sync door de gebruiker → readiness kwam weer binnen.
- **Toegangscode-slot voor delen met vrienden (2026-07-06):** de app kan achter een gedeelde toegangscode, als kostenbescherming vóór het delen met vrienden (voorkomt dat iedereen met de URL op kosten van de eigenaar Claude aanroept). Twee lagen: (1) `src/middleware.ts` — het échte slot — weigert álle `/api/*`-aanroepen met 401 zonder geldige `sc_access`-cookie; (2) `src/app/layout.tsx` checkt server-side dezelfde cookie en toont `src/components/LockScreen.tsx` (donker codescherm in app-stijl) zolang die ontbreekt. Code invoeren → `POST /api/access` (`src/app/api/access/route.ts`) valideert tegen env var `APP_ACCESS_CODE` en zet een httpOnly-cookie (1 jaar geldig). **Standaard uit:** zonder `APP_ACCESS_CODE` verandert er niets (middleware en layout laten alles door) — activeren = env var zetten op Vercel, deactiveren = verwijderen, code wijzigen = iedereen voert opnieuw in. Bewust één gedeelde code zonder accounts (echte multi-user auth is fase 2). Lokaal testen: launch-config `sportcoach-locktest` in `.claude/launch.json` (draait dev met `APP_ACCESS_CODE=test1234`).
- **Week-berekening tijdzone-fix (2026-07-06):** op de tweede maandag van de 2-wekelijkse cyclus toonde de app ten onrechte **week 1** i.p.v. week 2. Oorzaak in `src/lib/schedule.ts`: `getCurrentWeekNumber`/`getTrainingForDayOffset`/`getDaysInCurrentCycle` parseerden de `cycleStartDate` (`'YYYY-MM-DD'`) als **UTC-middernacht** maar namen `new Date()` als **lokale** tijd. In Amsterdam (UTC+1/+2) is "nu" vroeg op de dag in UTC nog de vorige dag, waardoor de dag-diff `6d23u` werd i.p.v. `7d` → `Math.floor` → 6 → `floor(6/7)=0` → week 1. Fix: twee helpers `amsterdamTodayUTC()` (vandaag als UTC-middernacht op de Amsterdamse kalenderdag, via `toLocaleDateString('en-CA', { timeZone: 'Europe/Amsterdam' })`) en `parseCycleStartUTC()`, in alle drie de functies gebruikt zodat beide kanten van de diff dezelfde UTC-basis hebben (exact geheel getal, klopt op de weekgrens). Zelfde UTC-aanpak als `alignCycleStartToRace` in `storage.ts`, die dit al deed. Tegelijk het negatieve-modulo-randgeval in `getDaysInCurrentCycle` gefixt (`((x % 14) + 14) % 14`). Puur read-time: geen opgeslagen plan/data gewijzigd, alleen "welke van de twee weken is nu actief".
- **Handmatige week-correctie + Schema-tab terug naar oude opzet (2026-07-08):** de automatische week-berekening zat er soms alsnog naast, dus er is nu een **handmatige escape**. (1) **Week-wissel-knop** op de Schema-tab (`src/components/SchemaContent.tsx`, onder de week-tabs): "Klopt de huidige week niet? Wissel week 1 ↔ 2" roept `toggleCycleWeekFlip()` aan. Opslag: nieuwe boolean-key `CYCLE_WEEK_FLIP` in `storage.ts` (`getCycleWeekFlip`/`toggleCycleWeekFlip`); `getActivePlan()` past bij een gezette flip een **−7-daagse verschuiving** toe op de uiteindelijke `cycleStartDate` (ná `alignCycleStartToRace`, dus de handmatige correctie wint altijd — ook over de race-uitlijning). Werkt door in de hele app (home/coach/schema) omdat alles `getActivePlan` gebruikt. (2) **Vandaag/morgen-detailsectie verwijderd** — de op 2026-06-26 toegevoegde `DetailedDay`/`DetailedSession`-kaarten bovenaan de Schema-tab zijn eruit (incl. bijbehorende helpers/imports); de tab opent weer direct met cyclus-info + week-tabs zoals daarvoor. (3) **Auto-scroll naar vandaag** — bij het openen scrollt de weekweergave weer automatisch naar de dag met de VANDAAG-markering (`todayRef` + `scrollIntoView({ block: 'center' })`, 1× per bezoek via `didScrollRef`, alleen in de huidige week; `scrollMarginTop` houdt rekening met de safe-area/vaste header). De flip reset `didScrollRef` zodat hij na het wisselen opnieuw naar vandaag scrolt. NB: op `main` handmatig gereconstrueerd zonder de personalisatie-code (die staat alleen op `dev`).
- **Home-tab "Volgens plan"-kaart: 7-daagse trainingsadherentie (2026-07-08):** de Home-tab (`src/components/HomeContent.tsx`) toont nu net boven "Volume deze week" een kaart die uitdrukt hoe goed de geplande trainingen van de **afgelopen 7 dagen** zijn uitgevoerd. Hergebruikt 1-op-1 `computeWeekAdherence()` uit `src/lib/training-load.ts` — dezelfde berekening die al op de Data-tab ("Volgens plan") draait: percentage gedane/geplande sessies + label (Sterk/Redelijk/Veel gemist, groen/amber/rood), gem. uitvoeringsscore, voortgangsbalk en een rij van 7 dagen met per geplande sessie een stip (groen = gedaan, rood = gemist, — = rustdag). Krachttraining telt niet mee (geen betrouwbare Garmin-registratie); vandaag telt niet mee (die training kan nog komen). Nieuwe `adherence`-useMemo (leest `getActivePlan` + `getActivityArchive`, gefilterd via `filterStatsActivities`) + een module-level `zonesForSport`-helper (sport-specifieke zones via `getRunZones`/`getCyclingZones`), zoals de Data-tab die ook heeft. Rendert alleen als er in het venster iets gepland stond én er Garmin-data is. NB: los naar `main` gecherrypickt (alleen dit onderwerp), zónder de personalisatie-code van de `dev`-branch.

## Git
- **Werkwijze (voorkeur gebruiker, 2026-05-31): bij UI-wijzigingen eerst een preview tonen en de gebruiker laten bevestigen dat het goed is; pas ná akkoord committen + pushen. Niet ongevraagd pushen.**
- **Dev-branch-werkwijze (2026-07-06, i.v.m. delen met vrienden):** werk NIET meer direct op `main`. Nieuwe wijzigingen gaan naar de **`dev`-branch**; Vercel bouwt daar automatisch een preview-deploy voor (URL te vinden op Vercel → Deployments, branch `dev`, of via de GitHub-deployment-status). De gebruiker bekijkt de preview-URL (op iPhone: in Safari; let op — het preview-adres heeft z'n eigen localStorage, dus geen Garmin-koppeling/doelen daar). Pas na akkoord mergen naar `main` (fast-forward of merge-commit), wat de productie-deploy triggert die vrienden gebruiken. Kleine doc-only wijzigingen (zoals CLAUDE.md) mogen direct op `main`.
- **Preview-screenshots bij design (voorkeur gebruiker, 2026-06-26):** stuur bij ontwerp-/UI-werk altijd een echte screenshot mee als preview (dev-server + headless Chromium → `SendUserFile`), niet alleen een tekstbeschrijving. De gebruiker vindt dit "erg fijn" en wil het standaard houden.
- Verifieer áltijd met `npx tsc --noEmit` + `npm run build` vóór de push.
- Push naar `main` → Vercel auto-deploy (productie).
- **Samenvatten bij merge naar main (voorkeur gebruiker, 2026-06-27): elke keer dat er iets naar `main` gemerged wordt, voeg een korte samenvatting van de wijziging toe aan CLAUDE.md** (een gedateerde entry onder "Belangrijke domeinkennis" in dezelfde stijl als de bestaande entries: wat is veranderd, welke bestanden/componenten, en het waarom). Commit die CLAUDE.md-update mee. Zo blijft dit bestand de canonieke changelog van de codebase.
- **"Unverified" commits op GitHub (2026-06-24):** de stop-hook waarschuwt soms dat een commit op GitHub als Unverified getoond wordt. Dit is **geen echt signing-probleem**: `git cat-file commit <sha>` laat zien dat er wél een geldige `gpgsig` (SSH-signature) in de commit staat, met de juiste committer-email (`noreply@anthropic.com`). `git log --show-signature` meldt lokaal "No signature"/een fout, maar dat komt alleen omdat `gpg.ssh.allowedSignersFile` hier niet is geconfigureerd — dat is puur een *lokale verificatie*-beperking, geen indicatie dat de signature ontbreekt. Als GitHub de commit toch als Unverified toont, ligt dat aan de registratie van de SSH-signing-key bij het GitHub-account (buiten bereik vanuit deze sandbox) — **niet** oplosbaar door te amenden/opnieuw te committen. Niet zonder overleg force-pushen om dit te "fixen".

## Verdere context
Uitgebreide feature-historie staat in het projectgeheugen onder
`~/.claude/projects/-Users-macbook2025taco-sportcoach-AI/memory/`.
